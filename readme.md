# Basic Setup for Typescript-Express Server

How to start:

- Clone the repository
- `npm i` to install dependencies
- `npm run build`
- `npm run start`

Available routes:

- /api
- /api/user
- /api/product

## Demo

https://ets-boilerplate-febriliankr.herokuapp.com/
